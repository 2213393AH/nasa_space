// This will handle the core game decision logic
export const gameLogic = (decision, scenario) => {
    if (decision === 'option1') {
      return 'positive';
    } else {
      return 'negative';
    }
  };
  